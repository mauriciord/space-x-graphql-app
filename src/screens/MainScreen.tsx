import React from 'react';
import { Text, View } from 'react-native';

const MainScreen: React.FC = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Main testing</Text>
    </View>
  );
};

export default MainScreen;
